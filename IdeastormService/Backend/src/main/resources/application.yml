spring:
  datasource:
    url: jdbc:postgresql://kimbaro.iptime.org:5432/board
    username: bookfit
    password: 1234
  jpa:
    hibernate:
#      ddl-auto: update
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  security:
    user:
      name: none
      password: none
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
server:
  port: 9020
